// Replace the values below with your Firebase project configuration.
// You can find these in Firebase Console -> Project Settings -> SDK setup and configuration -> Config

const FIREBASE_CONFIG = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  appId: "YOUR_APP_ID",
  // Optional items that may be present in your config:
  // messagingSenderId: "...",
  // storageBucket: "...",
  // measurementId: "..."
};
